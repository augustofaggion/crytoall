<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand fw-bold" href="#">CryptoAll</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <span class="nav-link text-white">Hello, <%= current_user.first_name %>!</span>
        </li>
        <li class="nav-item">
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-link text-white" %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container mt-5 text-center">
  <h1 class="text-primary fw-bold">Welcome to your Crypto Dashboard, <%= current_user.first_name %>!</h1>
  <p class="text-muted">Track your investments here.</p>

  <div class="mt-4">
    <%= link_to "View All Crypto", cryptos_path, class: "btn btn-lg btn-success" %>
  </div>
</div>
